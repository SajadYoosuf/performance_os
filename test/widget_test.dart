import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Performance OS app smoke test', (WidgetTester tester) async {
    // TODO: Add proper widget tests once Firebase is mocked
    expect(1 + 1, equals(2));
  });
}
